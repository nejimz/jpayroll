services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: payroll
      POSTGRES_PASSWORD: payroll
      POSTGRES_DB: payroll
    ports:
      - "5433:5432"
    volumes:
      - payroll_pg:/var/lib/postgresql/data

volumes:
  payroll_pg:
